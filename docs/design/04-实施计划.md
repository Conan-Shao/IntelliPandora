# 实施计划

> 排序原则：**基础自动化能力优先，LLM 靠后。**
>
> P0/P1 是纯内核工作，不依赖任何外部输入，可立即开工。P2 起需要真实业务域接入。

---

## 总览

| 阶段 | 主题 | 依赖业务域 | 核心产出 |
|---|---|---|---|
| **P0** | 清理与止血 | ❌ | 代码量 8415 → ~3000 LOC，全部可信；CI 建立 |
| **P1** | 内核基础能力 | ❌ | 断言层 + 可靠传输层——**框架首次具备"判定对错"的能力** |
| **P2** | 业务语义层 | ✅ | fixture / flow / 领域 invariant |
| **P3** | 入口层 | 部分 | runner 库调用 + MCP |
| **P4** | LLM 接入 | ❌ | 失败归因，默认关闭 |

---

## P0 · 清理与止血

**目标：把仓库变成一个可信的工作基础。** 当前 ~25% 是死代码或损坏，无 CI，有崩溃 bug 进过 release。

### 任务

| # | 任务 | 说明 |
|---|---|---|
| 0.1 | **归档 `core/engine/generator` 全栈** | 2737 LOC。**一个动作同时消灭三类债**：`case.py`/`testcase.py` 双模型、legacy 双渲染路径、39 个孤儿模块中的大部分。详见 [`01`](./01-现状评估与起点决策.md) 第五节 |
| 0.2 | 归档 RobotFramework 适配、`run/` 脚手架子命令 | 原方案 Part 6.1 |
| 0.3 | **清理硬编码凭据与内网域名** | `runtime.py:279-285` 的 `user_name`/`password` 硬编码（已确认失效，非紧急但必须清）；`yitu-inc.com`、`i5cnc.com` 两个前雇主域名 |
| 0.4 | **加 pre-commit 密钥扫描** | 防止再犯。机制本来就有——`runtime.py:66-70` 已支持 base64+AES 解密配置，只是没用上 |
| 0.5 | 修 `python_requires` → `>=3.10` | 当前声明 3.7 是错的，`testcaseserivce.py:176` 用了 PEP 604，3.7–3.9 上 import 即崩 |
| 0.6 | 修 `Makefile` test 路径 | 指向 `tests/`，实际是 `test/`，从未跑通 |
| 0.7 | 移除重复的 `scripts=` 声明 | `setup.py:71` 与 `entry_points` 重复 |
| 0.8 | **建 CI** | `pytest` + **import-all 冒烟**。后者是硬要求——两个 import 崩溃 bug 曾随 v1.1.0 发布，有冒烟测试必然拦下 |
| 0.9 | 统一 `runtime.py` 缩进为 4 空格 | 全仓库唯一的 Tab 文件 |
| 0.10 | 处置上一轮 `d831189` 改动 | 删 `cmp_semantic`、删 `create_test_case`；详见 [`03`](./03-LLM接入边界.md) 第五节 |

### 归档方式

不物理销毁。建议：打 tag `v1.1.0-archive` 或建 `archive/generator` 分支保留完整历史，再从主干删除。

### 验收

- [ ] `find src -name '*.py' | xargs -I{} python -c "import {}"` 全部通过（import-all 冒烟）
- [ ] CI 绿，且该冒烟测试在 CI 中常驻
- [ ] 全仓库 grep 不到硬编码凭据与内网域名
- [ ] pre-commit hook 能拦下一个测试用的假密钥
- [ ] 代码量降至约 3000 LOC

---

## P1 · 内核基础能力

**这是"先做好基础测试能力"的主体。** 不依赖业务域，可与 P0 部分并行。

按"挡不挡得住真实测试"排序：

### 1.1 断言层（最高优先级）

**现状：完全不存在。** 全 `src/` 无 `raise_for_status`；`status_code` 唯一用途是给日志上色。**接口返回 500，测试照常 PASS。**

这是当前最致命的缺口——**框架不具备判定对错的能力**，其他一切都建立在这之上。

按原方案 Part 2.3 建，新建 `core/assertion/`：

```python
@dataclass
class Check:
    name: str   # 中文，给人看
    ok:   bool
    expr: str   # 失败时的证据，不用翻日志
    src:  str   # onchain / api / schema / derived / authz
```

- 返回 `Check` 而非 raise → 一次跑完所有断言，一次看到所有失败
- `assert_all(*checks)` 收集器
- 基础判定：状态码、schema 一致性、字段存在性

> `utils/match.py` 的 `DictMatcher`/`Compare`（`$gt`/`$in`/`$contains`/...）已是一套可用的比较器分发机制，可作为底层复用。注意先删掉其中的 `cmp_semantic`。

### 1.2 传输层重写

| 问题 | 现状 | 目标 |
|---|---|---|
| TLS | `requestwrapper.py:82-84` 对所有 HTTPS 强制 `verify=False` | **默认 `True`**，需要关闭时显式声明 |
| 超时 | `apioption.py:22` 默认 0 且从不生效 → 无限等待 | **默认超时必须生效**（连接/读取分开） |
| 重试 | 无 | 可配置重试 + 指数退避，区分幂等/非幂等方法 |
| 错误语义 | 连接错误裸抛，非 2xx 无感知 | 连接错误 / 超时 / 非 2xx 各自可辨、可断言 |
| auth | `set_auth` 无任何调用方 | 接上，提供 bearer/basic/签名扩展点 |
| 连接池 | 仅 requests 默认 | `HTTPAdapter` 可配 |

**保留 `@api.http` / `@api.mark` 装饰器外观**——ergonomics 是好设计，换的是底下的实现。

### 1.3 响应解析修复

实测缺陷（见 [`01`](./01-现状评估与起点决策.md) 第 3.3 节）：

| 输入 | 当前 | 目标 |
|---|---|---|
| `{"code":0,"data":{...}}` | 返回 `['code','data']`（键名） | 返回业务数据 |
| `{"code":0,"data":null}` | `TypeError` | `None` |
| `{"code":0,"data":5}` | `TypeError` | `5` |
| 含连字符/关键字的键 | 静默降级成 dict，下游 `AttributeError` | 类型稳定 |
| 非 JSON（HTML/二进制） | 行为不一致 | 明确且一致 |

**原则：不静默降级。** 当前 `responsehandler.py:31-37` 把异常吞进 `logger.debug` 并改变返回类型——同一个方法的返回类型取决于响应内容长什么样，这比抛异常更难排查。

### 1.4 全局状态清理

- `Runtime.Case.steps`（`runtime.py:298-305`）：进程级全局 dict + 单一全局 key + 破坏性 `pop` 读取 → 改为运行上下文绑定
- `SessionManager`（`session.py:76-86`）：check-then-act 移入锁内；`acquire()`/`release()` 加 `try/finally`（当前任何异常都会永久死锁）
- `Runtime` 各 classproperty 的进程级懒缓存 → 明确生命周期

### 验收

- [ ] 一组针对**本地 mock server** 的框架自测，覆盖 1.1~1.4 每一条
- [ ] **零网络依赖**——当前唯一的测试打真实 httpbin.org，不可接受
- [ ] 一个返回 500 的接口，测试**必须 FAIL**（当前会 PASS）
- [ ] 一个挂起的接口，在超时后**失败而非挂死**
- [ ] `pytest -n 4` 并行跑同一套件，结果稳定

---

## P2 · 业务语义层

> ⚠️ **依赖真实业务域。** 见本文末尾"开放依赖"。

按原方案 Part 2 实施，此处只列顺序与关键约束。

### 2.1 Fixture（工程量最大）

- 账号池 + **借出加锁**（否则并行跑 nonce 直接撞）
- `ensure_*` **幂等归位**（跑两次结果一致）
- `reclaim` 回收 → 账号从消耗品变可循环资产
- **前置不满足要 SKIP 不能 FAIL**——原方案 Part 2.1 指出这是自动化跑久了最大的噪音源
- teardown 覆盖所有副作用
- 余额/资源告警

### 2.2 Flow

依赖链：`P1 传输层` → `client 生成（OpenAPI）` → `flow`

关键约束（原方案 Part 2.2）：链式返回 `self`；上下文走 `self.ctx`（`build()`/`send()` 应无参）；路径分支用策略模式不用 if/else；**不做 fallback**；负向场景走显式 override 口。

### 2.3 领域 Invariant

起步 5 条即可（原方案 Part 3.2）。**最省的起点：把已有测试脚本里的 assert 抽出来，合并同类项。**

> 这一步依赖既有脚本资产。若 pandora 仓库中有可复用的用例/断言，需在此前完成对比——见"开放依赖"。

### 验收

- [ ] 五条用例能并行跑十次，资源不漂移
- [ ] 前置不满足时是 SKIP 不是 FAIL
- [ ] 词汇表自检通过：用现有 flow+invariant 符号，能写出需求文档里每条验收标准
- [ ] 报告里 MISSING 归零

---

## P3 · 入口层

### 3.1 runner 改为可库调用

```python
# core/runner/api.py
def run(selector: str, env: str) -> RunResult: ...
```

pytest 插件与 MCP tool **都调它**，不 fork 第二套逻辑。

> ⚠️ 上一轮的 `run_test_suite` 是 `subprocess.run(['pytest', path])` + 解析 stdout——正是原方案 Part 4 明确反对的做法，需按此重做。

### 3.2 MCP stdio server

先做两个：`run_tests` + `explain_failure`。

**输出必须分叉**：给人的是 HTML 报告，给 agent 的是结构化摘要 + `run_id`。把 pytest 原始日志返给 agent 会直接淹掉 context。

> 原方案 Part 8 主张"MCP 不要放最后"，理由是越早暴露越早发现哪些 tool 真需要。本计划把它放在 P1 之后而非最前，原因是 `run_tests` 需要先有**值得跑的用例**和**判得出对错的断言**——在 P1 完成前，MCP 能暴露的只是一个 500 会 PASS 的套件。这是对原方案的一处有意偏离。

### 验收

- [ ] 删掉 `mcp/` 后 `pytest biz/<域>/cases` 毫发无伤
- [ ] 对话里说"跑一下 X 用例"能跑通并拿到可读摘要
- [ ] 返回的是摘要不是原始日志

---

## P4 · LLM 接入（默认关闭）

按 [`03-LLM接入边界.md`](./03-LLM接入边界.md) 实施。

- `explain_failure` 失败归因（flaky / 环境 / 数据 / 真 bug）
- 报告摘要

### 验收

- [ ] `pip install intellipandora` 不安装任何 LLM SDK
- [ ] 默认配置 `ai.enabled: false`
- [ ] CI 静态检查：`core/` 不 import `ai/`
- [ ] **`rm -rf src/ipandora/ai/ && pytest` 全绿**（CI 常驻 job）
- [ ] 预算闸生效；LLM 不可用时测试结论不受影响（fail-open）

---

## 开放依赖

以下输入不在本仓库，**P2 起需要**：

| 依赖 | 影响 |
|---|---|
| 垂直业务域的 OpenAPI spec | 无法生成 client，无法做 L0 契约测试 |
| 测试环境与账号池 | 无法做 fixture |
| pandora 仓库访问权限 | 无法确认是否有可复用的既有用例/断言（原方案"从现有脚本抽 assert"依赖此项） |

**P0/P1 不受影响，可立即开工。**

---

## 与原方案的差异说明

本计划基于 [`00-原始设计方案.md`](./00-原始设计方案.md)，有两处有意偏离，均已在上文标注理由：

| 差异 | 原方案 | 本计划 | 理由 |
|---|---|---|---|
| `core/protocol` 处置 | 列入"留" | **重写** | 实测成熟度 2/10：无断言层、`verify=False`、无超时、响应解析错误 |
| MCP 时机 | "不要放最后"，Phase 4 | 放在 P1 之后 | `run_tests` 需要先有判得出对错的断言层 |

新增了原方案未覆盖的 **P0 清理阶段**——原方案假定了一个健康的起点，实测不成立。
